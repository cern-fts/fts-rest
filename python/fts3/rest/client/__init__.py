from delegator import Delegator
from jobinquirer import JobInquirer
from exceptions import *
import logging

# Can be overridden by importers later on
logging.basicConfig(format='# %(message)s', level = logging.INFO)
